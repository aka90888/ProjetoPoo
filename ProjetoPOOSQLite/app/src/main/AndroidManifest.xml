<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mateus.correia.projetopoosqlite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Projeto"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TelaPrincipal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TelaCadastro" />
        <activity android:name=".TelaAluno" />
        <activity android:name=".TelaProfessor" />
        <activity android:name=".TelaListar" />
        <activity android:name=".TelaAlterar" />
        <activity android:name=".TelaProfessor2" />
        <activity android:name=".TelaCadastroAluno" />
        <activity android:name=".TelaCadastroProfessor" />
    </application>

</manifest>